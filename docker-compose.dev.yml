version: '3.7'

services:
  app:
    container_name: app-dev
    build:
      context: .
      dockerfile: Dockerfile.dev
    networks:
      - frontend
    volumes:
      - '.:/app'
      - '/app/node_modules'
    ports:
      - 3000:3000
networks:
  frontend:
    external: true
